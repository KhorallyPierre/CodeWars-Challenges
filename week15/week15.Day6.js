// Create a function that grabs the number of snacks from
// the input and tells you to stop that many times

function stopSnacking(snacks){
  for (let i =0; i < snacks.length; i ++){
    return'stop snacking!!'
  }
}
console.log(stopSnacking(20))
